#![allow(unused)]

mod tasks;

fn main() {
    tasks::one::part_one();
    tasks::one::part_two();
    tasks::two::part_one();
    tasks::two::part_two();
    tasks::three::part_one();
    tasks::three::part_two();
}
